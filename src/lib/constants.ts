// Constants for CatchLog

// File Upload
export const MAX_FILE_SIZE = 10 * 1024 * 1024; // 10MB
export const ALLOWED_IMAGE_TYPES = ['image/jpeg', 'image/png', 'image/webp', 'image/heic'];

// Image Processing
export const IMAGE_MAX_WIDTH = 1200;
export const IMAGE_MAX_HEIGHT = 1200;
export const IMAGE_QUALITY = 'auto';

// Debounce Timing
export const DEBOUNCE_DELAY_MS = 800;

// GPS
export const GPS_TIMEOUT_MS = 10000;
export const GPS_HIGH_ACCURACY = true;

// Map
export const DEFAULT_MAP_ZOOM = 13;
export const USER_LOCATION_ZOOM = 14;
export const MAX_CLUSTER_RADIUS = 50;

// Pagination
export const DEFAULT_PAGE_SIZE = 20;

// Cache
export const WEATHER_CACHE_MINUTES = 30;
